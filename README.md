# SpeakEstonian

Next.js app for the SpeakEstonian learning experience (static UI; no backend in this repo).

## Development

```bash
pnpm install
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000).

## Build

```bash
pnpm build
pnpm start
```
